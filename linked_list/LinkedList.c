#include<stdio.h>
int main (){
    node struct {
        int data;
        node struct* next;
    }
    node* N(){
        *next = (struct*)malloc(sizeof(node struct));
        return *next;
    }
    return 0;
}